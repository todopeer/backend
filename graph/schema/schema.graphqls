scalar Date

directive @auth on FIELD_DEFINITION

type Query {
	ping: Boolean!
}

input PomoInput {
	taskId: ID!
}

type AuthPayload {
	user: User!
	token: String!
}

type Mutation {
	pomoStart(input: PomoInput!): Event! @auth
	pomoPause(input: PomoInput!): Event! @auth
}